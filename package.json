{
  "name": "solix",
  "version": "2.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "@vitest/coverage-v8": "^2.1.0",
    "jsdom": "^30.0.1",
    "@types/jsdom": "^21.1.7"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  }
}
